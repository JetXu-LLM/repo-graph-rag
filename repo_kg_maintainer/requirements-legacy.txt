-r requirements.txt
# Legacy full-build extras keep Arango persistence and llama-github-backed
# repository discovery/filtering in the loop.
python-arango
python-dotenv
# llama-github 0.4.0 publishes the modern LangChain provider dependencies it
# needs, so this file no longer mirrors those transitive pins directly.
llama-github==0.4.0
